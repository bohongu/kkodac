import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return <HeaderWrapper>꼬닥꼬닥</HeaderWrapper>;
};

export default Header;

const HeaderWrapper = styled.div`
  width: 100%;
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: end;
  padding-right: 80px;
  font-size: 30px;
`;
